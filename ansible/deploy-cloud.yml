---
- name: deploy cloud
  hosts: localhost
  gather_facts: false
  vars:
    gcp_project: pjds-fog-grouping-326209
    gcp_cred_kind: serviceaccount
    gcp_cred_file: "{{ playbook_dir | dirname }}/credentials/pjds-fog-grouping-326209-8db0019fa804.json"
    zone: "europe-west3-a"
    region: "europe-west3"
  roles:
    - deploy_cloud

- name: Manage cloud
  hosts: cloudgroup
  vars:
    ansible_ssh_private_key_file: "{{ playbook_dir | dirname }}/credentials/ssh-key-ansible-sa"
  connection: ssh
  remote_user: sa_103183904426379622520
  become: True
  roles:
    - update
    - python3
    - basic