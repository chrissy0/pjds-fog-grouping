version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  register-cluster:
    lang: python3
    handler: ./register-cluster
    image: pjdsgrouping/register-cluster:latest

